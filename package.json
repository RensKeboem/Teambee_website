{
  "name": "teambee-website",
  "version": "1.0.0",
  "description": "Teambee website with Tailwind CSS",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./public/app.css --minify",
    "build": "npm run build:css",
    "dev": "tailwindcss -i ./src/input.css -o ./public/app.css --watch"
  },
  "dependencies": {
    "tailwindcss": "^3.4.1"
  }
}
